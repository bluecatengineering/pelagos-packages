export const bisector = jest.fn().mockReturnValue({left: jest.fn()});

export const max = jest.fn();

export const min = jest.fn();

export const median = jest.fn();

export const extent = jest.fn();

export const sum = jest.fn();
