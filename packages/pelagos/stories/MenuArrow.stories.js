import {MenuArrow} from '../src';

const Template = (args) => <MenuArrow {...args} />;

export const Default = Template.bind({});
Default.args = {};

export default {
	title: 'Components/MenuArrow',
	component: MenuArrow,
};
