import {Meta} from '@storybook/addon-docs/blocks';
import fontData from '../../defs/fonts.yaml';

<Meta title="Styles/Fonts" />

# Fonts

See the "Including fonts in the page" section below.

<table>
	<thead>
		<tr>
			<th>Token</th>
			<th>Usage</th>
			<th>Specs</th>
		</tr>
	</thead>
	<tbody>
		{Object.entries(fontData.fonts)
			.filter(([, v]) => !v.deprecated)
			.map(
				([
					key,
					{
						use,
						specs: {fontFamily, fontSize, lineHeight, fontWeight, letterSpacing},
					},
				]) => (
					<tr key={key} style={{verticalAlign: 'top'}}>
						<td style={{fontFamily, fontSize, lineHeight, fontWeight, letterSpacing}}>{key}</td>
						<td>{use}</td>
						<td style={{whiteSpace: 'nowrap'}}>
							Family: {fontFamily}
							<br />
							Size: {parseFloat(fontSize) * 16}px / {fontSize}
							<br />
							Line height: {parseFloat(lineHeight) * 16}px / {lineHeight}
							<br />
							Weight: {fontWeight}
							<br />
							Letter spacing: {letterSpacing}
						</td>
					</tr>
				)
			)}
	</tbody>
</table>

<br />

## Including fonts in the page

You can load these fonts from Google Fonts by adding the following lines to your HTML file.

```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" />
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto+Condensed:wght@400;600&display=swap" />
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inconsolata&display=swap" />
```

If needed, you can also embed the font files in the application. In that case, include the same font families and weights shown above.
