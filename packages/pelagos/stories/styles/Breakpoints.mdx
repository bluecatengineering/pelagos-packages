import {Meta} from '@storybook/addon-docs';
import breakpoints from '../../defs/breakpoints.yaml';

<Meta title="Styles/Breakpoints" />

# Breakpoints

<table>
	<thead>
		<tr>
			<th>Breakpoint</th>
			<th>min-width (px)</th>
			<th>min-width (rem)</th>
		</tr>
	</thead>
	<tbody>
		{Object.entries(breakpoints).map(([key, size]) => (
			<tr key={key}>
				<td>{key}</td>
				<td>{size}</td>
				<td>{size / 16}</td>
			</tr>
		))}
	</tbody>
</table>
