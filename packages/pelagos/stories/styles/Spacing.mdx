import {Meta} from '@storybook/addon-docs/blocks';
import spacing from '../../defs/spacing.yaml';
import './Spacing.less';

<Meta title="Styles/Spacing" />

# Spacing

<table>
	<thead>
		<tr>
			<th>Token</th>
			<th>Factor</th>
			<th>Sample</th>
		</tr>
	</thead>
	<tbody>
		{Object.entries(spacing).map(([key, size]) => (
			<tr key={key}>
				<td>{key}</td>
				<td>{size / 16}</td>
				<td>
					<div className="Spacing__sample" style={{width: size}} />
				</td>
			</tr>
		))}
	</tbody>
</table>
