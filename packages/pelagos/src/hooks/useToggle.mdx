{/* DO NOT EDIT, generated from useToggle.js */}

import {Meta} from '@storybook/addon-docs/blocks';

<Meta title="hooks/useToggle" />

# useToggle

Returns a stateful boolean and a function to toggle it.
Additionally, adds an effect which sets the state to true if any errors are found.

## Parameters

<table>
<thead><tr><th>Param</th><th>Type</th><th>Description</th></tr></thead>
<tbody>
<tr><td>initialState</td><td>`boolean`</td><td>the initial state.</td></tr>
<tr><td>errors</td><td>`Array.<any>`</td><td>the array of errors.</td></tr>
</tbody>
</table>

## Returns

`useToggle~Return` - tuple which can be used to control a collapsible.

## Example

```js
import {Collapsible, useToggle} from '@bluecateng/pelagos';

const Example = ({errors}) => {
	const [open, toggleOpen] = useToggle(false, errors);
	return (
		<Collapsible open={open} onHeaderClick={toggleOpen}>
			...
		</Collapsible>
	);
};
```

## Other types

### useToggle~Return

`Array` - return type for `useToggle`.

#### Properties

<table>
	<thead>
		<tr>
			<th>Name</th>
			<th>Type</th>
			<th>Description</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td>0</td>
			<td>`boolean`</td>
			<td>the current value.</td>
		</tr>
		<tr>
			<td>1</td>
			<td>`function`</td>
			<td>function to toggle the value.</td>
		</tr>
	</tbody>
</table>
