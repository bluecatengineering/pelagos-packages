{/* DO NOT EDIT, generated from useFocusTrap.js */}

import {Meta} from '@storybook/addon-docs';

<Meta title="hooks/useFocusTrap" />

# useFocusTrap

Adds an effect which creates a focus trap.

## Parameters

<table>
	<thead>
		<tr>
			<th>Param</th>
			<th>Type</th>
			<th>Description</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td>ref</td>
			<td>`Object`</td>
			<td>React reference to the target element.</td>
		</tr>
		<tr>
			<td>initialFocus</td>
			<td>`string`</td>
			<td>the CSS selector of the element which receives the focus when the trap is activated.</td>
		</tr>
		<tr>
			<td>onDeactivate</td>
			<td>`function`</td>
			<td>invoked when the trap is deactivated.</td>
		</tr>
	</tbody>
</table>

## Example

```js
import {useRef} from 'react';
import {useFocusTrap} from '@bluecateng/pelagos';

const Example = () => {
	const ref = useRef(null);
	useFocusTrap(ref);
	return <div ref={ref}>...</div>;
};
```
