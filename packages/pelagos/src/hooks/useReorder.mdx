{/* DO NOT EDIT, generated from useReorder.js */}

import {Meta} from '@storybook/addon-docs';

<Meta title="hooks/useReorder" />

# useReorder

Returns two React refs which enable reordering child nodes using drag and drop.

## Parameters

<table>
	<thead>
		<tr>
			<th>Param</th>
			<th>Type</th>
			<th>Description</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td>selector</td>
			<td>`string`</td>
			<td>CSS selector for elements to reorder.</td>
		</tr>
		<tr>
			<td>handleSelector</td>
			<td>`string`</td>
			<td>CSS selector for reorder handle.</td>
		</tr>
		<tr>
			<td>count</td>
			<td>`number`</td>
			<td>the number of elements.</td>
		</tr>
		<tr>
			<td>getElementName</td>
			<td>`function`</td>
			<td>returns the name of the specified element.</td>
		</tr>
		<tr>
			<td>onChange</td>
			<td>`function`</td>
			<td>
				invoked when the list is changed, the first argument is the original index of the element moved, the second is
				the target index.
			</td>
		</tr>
	</tbody>
</table>

## Returns

`Array.<MutableRefObject.<Element>>` - reorderRef and liveRef.

## Example

```js
import {useCallback} from 'react';
import {useReorder} from '@bluecateng/pelagos';

const Example = ({list}) => {
	const getElementName = useCallback((element) => {
		// return name for element
	}, []);
	const onChange = useCallback((fromIndex, toIndex) => {
		// move item
	}, []);
	const [reorderRef, liveRef] = useReorder('.Child', '.Handle', list.length, getElementName, onChange);
	return (
		<div>
			<div className="sr-only" aria-live="polite" ref={liveRef} />
			<ol ref={reorderRef}>...</ol>
		</div>
	);
};
```
