{/* DO NOT EDIT, generated from getGenericSuggestions.js */}

import {Meta} from '@storybook/addon-docs';

<Meta title="functions/getGenericSuggestions" />

# getGenericSuggestions

Returns suggestions from a list.

## Parameters

<table>
<thead><tr><th>Param</th><th>Type</th><th>Description</th></tr></thead>
<tbody>
<tr><td>text</td><td>`string`</td><td>the text to search in the list.</td></tr>
<tr><td>selected</td><td>`Array.<{id: *}> / function`</td><td>the list of options already selected or a function to check if already selected.</td></tr>
<tr><td>sourceList</td><td>`Array.<{id: *, name: string}>`</td><td>the list where options are searched.</td></tr>
<tr><td>errorMessage</td><td>`string`</td><td>the error message to show if no suggestions are found.</td></tr>
</tbody>
</table>

## Returns

`Object / Object` - the suggestions.

## Example

```js
import {ListInput, getGenericSuggestions} from '@bluecateng/pelagos';

const Example = ({items}) => {
	const getSuggestions = useCallback(
		(text, list) => getGenericSuggestions(text, list, items, t`No matching items found`),
		[items]
	);
	return <ListInput getSuggestions={getSuggestions} />;
};
```
