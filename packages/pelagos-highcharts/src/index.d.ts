/**
 * Returns a class name appropriate for a subset of colors.
 * @param className an optional additional class name.
 * @param count the number of items displayed in the chart.
 * @param option an optional alternate color set.
 */
export const buildColorSetClass: (className?: string, count?: number | null, option?: number) => string | undefined;
